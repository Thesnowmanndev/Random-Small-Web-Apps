import questions from "../data/questions.json" assert { type: "json" };
import answers from "../data/answers.json" assert { type: "json" };

export const blizzardTitle = "Blizzard";

export let blizzardQuestions = questions.blizzardQuestions;
export let blizzardAnswers = answers.blizzardAnswers;
